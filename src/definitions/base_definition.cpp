#include "base_definition.hpp"

namespace space
{
    BaseDefinition::BaseDefinition(const DefinitionId &id) : id(id)
    {

    }

    void BaseDefinition::on_post_load(Engine &engine)
    {

    }
}